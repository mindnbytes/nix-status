{
  "settings": {
    "flake_dir": {
      "label": "Flake directory",
      "description": "Select the directory containing flake.nix. Required for input updates, which modify this directory's flake.lock."
    },
    "use_themed_logos": {
      "label": "Use themed NixOS logos",
      "description": "Use rendered SVGs from the optional bundled templates (see README). Falls back to the snowflake glyph when a logo file is missing."
    }
  }
}
